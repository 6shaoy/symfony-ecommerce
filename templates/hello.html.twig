<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hello !</title>
</head>
<body>
    <h1>Hello {{ prenom }}</h1>

    <ul>
        {% for age in ages %}
            <li>
                {% if age >= 18 %}
                    adult
                {% else %}
                    enfant
                {% endif %}
            </li>
        {% endfor %}
    </ul>
</body>
</html>